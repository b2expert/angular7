import { SimpleSliderDirective } from './simple-slider.directive';

describe('SimpleSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleSliderDirective();
    expect(directive).toBeTruthy();
  });
});
