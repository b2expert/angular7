import { NavRestrictDirective } from './nav-restrict.directive';

describe('NavRestrictDirective', () => {
  it('should create an instance', () => {
    const directive = new NavRestrictDirective();
    expect(directive).toBeTruthy();
  });
});
